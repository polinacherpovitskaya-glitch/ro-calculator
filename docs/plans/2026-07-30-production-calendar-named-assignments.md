# Plan — Именные назначения в производственном календаре

**Spec:** `docs/specs/2026-07-30-production-calendar-named-assignments.md`  
**Created:** 2026-07-30  
**Status:** Ready for execution

## Execution analysis

- Задача разделена на две волны: сначала именной план, затем закрытие смены.
- Волна A не требует миграции БД: сотрудники уже загружаются из `employees`,
  а назначения и цвета можно добавить в `production_plan_state_json`.
- Дневные часы можно показать сразу из готового schedule.
- Дневные штуки допустимы только для заказов с надёжными
  `production_quantity` и плановыми часами этапа.
- Вечерний факт намеренно не входит в этот PR: сначала нужно подтвердить на
  реальных заказах, что плановая норма в штуках не вводит цех в заблуждение.

## Assumptions

- Канонический состав календаря — активные сотрудники с `role=production`.
- При ровно четырёх сотрудниках отдельная первоначальная настройка не нужна.
- Ручное назначение является жёстким; автоматическое — свободно
  перераспределяется после изменения очереди.
- Верхняя очередь и выдвижная панель удобнее постоянной левой колонки.
- Текущая модель человеко-часов остаётся источником прогнозных дат.

## Milestone order

| ID | Title | Depends on | Status |
| --- | --- | --- | --- |
| M1 | Уникальная визуальная идентичность заказов | - | [ ] |
| M2 | Верхняя очередь и панель редактирования | M1 | [ ] |
| M3 | Состав команды и именные назначения | M2 | [ ] |
| M4 | Дневные часы и безопасная норма в штуках | M3 | [ ] |
| M5 | Регрессия, визуальная проверка и версия | M1–M4 | [ ] |

## M1. Уникальная визуальная идентичность заказов `[ ]`

### Goal

Каждый видимый заказ узнаваем по собственному акценту, а соседние заказы не
сливаются.

### Tasks

- [ ] Добавить нормализацию `order_color_slots`.
- [ ] Построить детерминированную карту цветов для текущей очереди.
- [ ] Исключить повтор цвета среди видимых заказов.
- [ ] Максимизировать дистанцию между четырьмя ближайшими заказами.
- [ ] Использовать одну карту в очереди, полосах, скобах и дедлайнах.
- [ ] Добавить smoke fixtures для очереди длиннее старой палитры.

### Definition of done

- Десять и более соседних заказов имеют разные акценты.
- Перестановка двух заказов не меняет их собственные цвета.
- Этапы остаются узнаваемыми по текущим цветам заливки.

### Validation

```sh
node --check js/gantt.js
node tests/production-calendar-smoke.js
```

### Known risks

- Слишком светлый сгенерированный цвет может потерять контраст.

### Stop-and-fix rule

- Не переходить к новой компоновке, пока fixture не доказывает уникальность и
  минимальный контраст.

## M2. Верхняя очередь и панель редактирования `[ ]`

### Goal

Освободить ширину календаря без потери удобного управления приоритетом.

### Tasks

- [ ] Добавить компактную верхнюю полосу очереди.
- [ ] Перенести подробные карточки в выдвижную панель.
- [ ] Сохранить drag-and-drop, прогноз, дедлайн и открытие заказа.
- [ ] Удалить левую колонку и resizer только после функционального паритета.
- [ ] Добавить управление панелью с клавиатуры и возврат фокуса.
- [ ] Проверить горизонтальный overflow и мобильные две строки.

### Definition of done

- В обычном режиме календарь использует всю ширину контента.
- Очередь редактируется за один клик без перехода на другую страницу.
- Все действия старой левой панели доступны в новой панели.

### Validation

```sh
node --check js/gantt.js
node tests/production-calendar-smoke.js
```

### Known risks

- Drag внутри прокручиваемой панели может конфликтовать со скроллом.

### Stop-and-fix rule

- Не удалять старую панель, пока тест не подтверждает полный паритет действий.

## M3. Состав команды и именные назначения `[ ]`

### Goal

Каждая линия принадлежит реальному сотруднику, а начальник производства
назначает конкретных людей.

### Tasks

- [ ] Передать employees в модель отображения, а не только в расчёт факта.
- [ ] Нормализовать `roster_employee_ids` и `employee_assignments`.
- [ ] Автоматически выбрать ровно четыре активных production-сотрудника.
- [ ] Добавить выбор состава, если активных сотрудников больше или меньше.
- [ ] Заменить `Человек 1–4` именами и дневными часами.
- [ ] Добавить `Авто` и ручные чипы исполнителей в очередь.
- [ ] Синхронизировать ручное количество с `parallel_workers`.
- [ ] Обновить scheduler: worker slot получает стабильный `employeeId`.
- [ ] Зафиксировать ожидание закреплённого занятого сотрудника.
- [ ] Помечать невалидное назначение как требующее переназначения.

### Definition of done

- Можно назначить один заказ двум конкретным людям.
- Другие два человека получают следующие заказы по очереди.
- На каждой именной линии отсутствуют пересечения.
- После reload имена, порядок линий и ручные назначения сохраняются.

### Validation

```sh
node --check js/production-core.js
node --check js/gantt.js
node tests/production-calendar-smoke.js
node tests/employee-auth-payroll-smoke.js
```

### Known risks

- Старые cached employees могут кратко отобразиться до удалённого обновления.
- Неактивный сотрудник может остаться в сохранённом назначении.

### Stop-and-fix rule

- Любое пересечение именной линии или тихая подмена ручного исполнителя
  блокирует переход к дневным нормам.

## M4. Дневные часы и безопасная норма в штуках `[ ]`

### Goal

Сотрудник без расчётов понимает своё сегодняшнее задание.

### Tasks

- [ ] Суммировать сегодняшние интервалы по сотруднику, заказу и этапу.
- [ ] Показывать плановые часы рядом с именем и в сегодняшнем баре.
- [ ] Рассчитать плановые штуки из `production_quantity` и часов этапа.
- [ ] Распределить округление без превышения количества заказа.
- [ ] Выключать штуки для неоднозначных или неполных данных.
- [ ] Показывать `Нужна норма` вместо ложного числа.
- [ ] Добавить структуру `daily assignment`, пригодную для следующей волны
  закрытия смены, без сохранения факта.

### Definition of done

- На однородном заказе видно `9 ч · план N шт.`.
- На неоднозначном заказе видно время и честный статус отсутствия нормы.
- Сумма плановых единиц этапа не превышает количество заказа.

### Validation

```sh
node --check js/production-core.js
node --check js/gantt.js
node tests/production-calendar-smoke.js
```

### Known risks

- Агрегат `production_quantity` может быть недостаточно точным для заказа с
  несколькими разными изделиями.

### Stop-and-fix rule

- При сомнительной структуре заказа скрыть штуки, а не выпускать приблизительное
  число без маркировки.

## M5. Регрессия, визуальная проверка и версия `[ ]`

### Goal

Подготовить безопасный отдельный релиз волны A.

### Tasks

- [ ] Прогнать JS syntax checks.
- [ ] Прогнать production calendar, partial delivery и order-flow smokes.
- [ ] Проверить 1–4 ручных исполнителей в реальном браузере.
- [ ] Проверить week/month и desktop/mobile layout.
- [ ] Проверить очередь из 12+ заказов и четыре соседних цвета.
- [ ] Проверить reload persistence.
- [ ] Прочитать актуальную версию `origin/main` перед bump.
- [ ] Обновить четыре version anchors и cache-bust изменённых файлов.
- [ ] Проверить diff на случайные API, schema и selector changes.

### Definition of done

- Все проверки из test plan зелёные.
- Версия согласована в четырёх anchors.
- Нет пересечений, повторов цвета и потери старых планов.

### Validation

```sh
for f in js/*.js; do node --check "$f"; done
node tests/production-calendar-smoke.js
node tests/partial-delivery-smoke.js
node tests/order-flow-smoke.js
node tests/employee-auth-payroll-smoke.js
node tests/version-smoke.js
```

### Known risks

- Параллельные изменения `main` могут занять выбранный номер версии.

### Stop-and-fix rule

- При любом провале исправить текущий milestone и повторить его проверки до
  перехода дальше.

## Follow-up

После реальной проверки дневных штук создать отдельные spec/plan для волны B:
вечернее закрытие смены и сохранение фактических единиц в meta существующих
time entries.

## Rollback

Откатить frontend-коммиты волны A. Новые необязательные поля
`production_plan_state_json` старые клиенты игнорируют; миграция данных не
нужна.
