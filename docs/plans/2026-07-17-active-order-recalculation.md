# План: пересчёт активных заказов по текущим нормам

Связанная спека: [`../specs/2026-07-17-active-order-recalculation.md`](../specs/2026-07-17-active-order-recalculation.md)

## 1. Единая норма запуска и защита кастомов

- [x] В движке выбрать норму бланка/кастома, если нет индивидуального override.
- [x] Учесть 2 ч на каждую кастомную форму и 0,5 ч на каждый цвет; для бланка
  оставить только 0,5 ч на цвет.
- [x] Не применять каталоговую скорость к кастому только из-за `template_id`.
- [x] Закрыть правила unit/smoke-тестом.

## 2. Воспроизводимый пересчёт первой волны

- [x] Добавить скрипт dry-run для образцов и production со строгой
  валидацией снимков и агрегированным отчётом.
- [x] Добавить `--apply`: локальный backup, обновление шапки и расчётных
  полей позиций, повторное чтение и проверка результата.
- [x] Сделать отдельный scope для черновиков, не применяя его в первой волне.
- [x] Добавить тест скрипта на бланк, кастом и скидку.
- [x] Пропускать только полностью пустые технические строки черновиков;
  содержательные незаполненные позиции оставлять блокером.
- [x] Разрешать проверенные исторические ссылки ID 30 → 31 и ID 18 → 15 через
  действующие бланки только в расчётном снимке, не переписывая состав заказа.
- [x] Безопасно отделять черновики без назначенной нормы для ручной доработки,
  не блокируя остальные корректные заказы второй волны.
- [x] При пересчёте себестоимости сохранить согласованную ненулевую выручку
  коммерческого заказа; заполнять выручку только у нулевого черновика с
  явными ценами позиций.

## 3. Понятная шкала квартальной загрузки

- [x] Добавить чистую функцию трёх месяцев квартала и тест её границ.
- [x] Показать две тонкие чёрные границы, подписи месяцев и текущий месяц.
- [x] Убрать из основного текста неоднозначное «сегодня N%».

## 4. Проверка и выпуск

- [x] Выполнить dry-run на production и проверить, что нет unsafe-позиций.
- [x] Поднять версию и cache-bust изменённых браузерных файлов.
- [x] Прогнать syntax, unit/smoke и production preflight.
- [ ] Выпустить код; затем применить первую волну и повторно проверить данные.
