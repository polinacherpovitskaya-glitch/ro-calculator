# Block 2 — Auth + employees Implementation Plan

> **REQUIRED:** Прочитай мастер-плейбук [`2026-05-15-MIGRATION-PLAYBOOK.md`](2026-05-15-MIGRATION-PLAYBOOK.md) перед стартом. Использует ту же инфру что Block 1.

**Goal:** Сотрудники могут залогиниться в `ops-staging.recycleobject.ru`. Cookie-based сессия. Скопированы данные `employees` из Supabase. Middleware защищает все будущие API-эндпойнты.

**Architecture:** Argon2id для паролей, HttpOnly + Secure cookie `session_id` (TTL 60 дней), сессии хранятся в Postgres. Роли `admin` / `user`. При миграции — временный пароль на сотрудника, меняется при первом входе.

**Tech additions vs Block 1:** `argon2` npm package, `cookie-parser` middleware.

**Dependencies:** Block 1 (Tasks 1-11 завершены и смержены).

**Branch:** `git checkout main && git pull && git checkout -b block-2-auth`

---

## File Structure

| File | Action | Responsibility |
|------|--------|----------------|
| `ops/db/migrations/002_auth.sql` | **Create** | Таблицы `employees`, `auth_users`, `auth_sessions`, `idempotency_keys` |
| `ops/api/package.json` | **Modify** | Add `argon2`, `cookie-parser` |
| `ops/api/src/db.js` | **Modify** | Helper `withTransaction()` для атомарных операций |
| `ops/api/src/auth/argon.js` | **Create** | `hashPassword(plain)`, `verifyPassword(plain, hash)` |
| `ops/api/src/auth/sessions.js` | **Create** | `createSession(userId, meta)`, `loadSession(id)`, `revokeSession(id)` |
| `ops/api/src/middleware/auth.js` | **Create** | Express middleware: читает cookie, цепляет `req.user`, возвращает 401 если нет сессии |
| `ops/api/src/routes/auth.js` | **Create** | `POST /api/auth/login`, `POST /api/auth/logout`, `GET /api/auth/me`, `POST /api/auth/change-password` |
| `ops/api/src/routes/employees.js` | **Create** | `GET /api/employees` (требует auth, возвращает список) |
| `ops/api/src/server.js` | **Modify** | Подключить `cookie-parser`, `routes/auth`, `routes/employees` |
| `ops/api/test/auth.test.js` | **Create** | Unit + integration тесты для login/logout/me/change-password |
| `ops/api/test/employees.test.js` | **Create** | Тесты что без cookie → 401, с cookie → 200 |
| `ops/scripts/refresh/01-employees.mjs` | **Create** | Копирует `employees` из Supabase в Postgres |
| `ops/scripts/issue-temp-passwords.mjs` | **Create** | Генерирует временные пароли для всех `is_active` employees, выводит CSV |
| `ops/web/src/views/LoginView.vue` | **Create** | Экран логина |
| `ops/web/src/views/ChangePasswordView.vue` | **Create** | Экран смены пароля (вызывается при `must_change_password=true`) |
| `ops/web/src/views/PlaceholderView.vue` | **Modify** | Использовать `useAuthStore` — показывать имя залогиненного |
| `ops/web/src/stores/auth.ts` | **Create** | Pinia store: `user`, `login()`, `logout()`, `me()` |
| `ops/web/src/api/auth.ts` | **Create** | Обёртки над fetch к `/api/auth/*` |
| `ops/web/src/router.ts` | **Modify** | Маршруты `/login`, `/change-password`, navigation guard |

---

## Task 1: SQL миграция таблиц auth + employees

**Files:** Create `ops/db/migrations/002_auth.sql`

- [ ] Write the migration file:

```sql
-- 002_auth.sql
-- Auth + employees + idempotency. Скопировано из Supabase employees сохраняет
-- те же ID, что и в старой системе.

CREATE TABLE IF NOT EXISTS employees (
    id              INTEGER PRIMARY KEY,
    name            TEXT NOT NULL,
    email           TEXT,
    role            TEXT,
    hourly_rate     NUMERIC,
    is_active       BOOLEAN NOT NULL DEFAULT TRUE,
    extras          JSONB DEFAULT '{}'::jsonb,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_employees_active ON employees(is_active);
CREATE INDEX IF NOT EXISTS idx_employees_email ON employees(LOWER(email));

CREATE TABLE IF NOT EXISTS auth_users (
    id                     SERIAL PRIMARY KEY,
    email                  TEXT NOT NULL UNIQUE,
    password_hash          TEXT NOT NULL,
    employee_id            INTEGER REFERENCES employees(id) ON DELETE SET NULL,
    role                   TEXT NOT NULL DEFAULT 'user' CHECK (role IN ('admin','user')),
    must_change_password   BOOLEAN NOT NULL DEFAULT TRUE,
    created_at             TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    last_login_at          TIMESTAMPTZ
);
CREATE INDEX IF NOT EXISTS idx_auth_users_email ON auth_users(LOWER(email));

CREATE TABLE IF NOT EXISTS auth_sessions (
    id             TEXT PRIMARY KEY,
    user_id        INTEGER NOT NULL REFERENCES auth_users(id) ON DELETE CASCADE,
    created_at     TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    expires_at     TIMESTAMPTZ NOT NULL,
    ip             TEXT,
    user_agent     TEXT,
    revoked_at     TIMESTAMPTZ
);
CREATE INDEX IF NOT EXISTS idx_auth_sessions_user ON auth_sessions(user_id);
CREATE INDEX IF NOT EXISTS idx_auth_sessions_expires ON auth_sessions(expires_at);

CREATE TABLE IF NOT EXISTS idempotency_keys (
    key             TEXT PRIMARY KEY,
    user_id         INTEGER REFERENCES auth_users(id) ON DELETE SET NULL,
    method          TEXT NOT NULL,
    path            TEXT NOT NULL,
    response_status INTEGER NOT NULL,
    response_body   TEXT NOT NULL,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_idempotency_keys_created ON idempotency_keys(created_at);

INSERT INTO app_meta (id, version) VALUES (1, '002-auth')
ON CONFLICT (id) DO UPDATE SET version = EXCLUDED.version, applied_at = NOW();
```

- [ ] Apply migration to dev DB:

```bash
DATABASE_URL="postgres://ops:ops_dev_password@127.0.0.1:5433/ops" ops/db/migrate.sh
```

Expected: `Migrations complete.` Check `psql ... -c "SELECT version FROM app_meta;"` → `002-auth`.

- [ ] Commit: `Add auth + employees + idempotency tables`

---

## Task 2: Установить argon2 и cookie-parser, добавить helpers

**Files:** Modify `ops/api/package.json`, Create `ops/api/src/auth/argon.js`, modify `ops/api/src/db.js`

- [ ] In `ops/api/package.json` dependencies add:

```json
"argon2": "^0.41.1",
"cookie-parser": "^1.4.6"
```

Run `cd ops/api && npm install`.

- [ ] Create `ops/api/src/auth/argon.js`:

```js
import argon2 from 'argon2';

const HASH_OPTIONS = {
  type: argon2.argon2id,
  memoryCost: 2 ** 16,
  timeCost: 3,
  parallelism: 1,
};

export async function hashPassword(plain) {
  if (typeof plain !== 'string' || plain.length === 0) {
    throw new Error('hashPassword: plain must be a non-empty string');
  }
  return argon2.hash(plain, HASH_OPTIONS);
}

export async function verifyPassword(plain, hash) {
  if (typeof plain !== 'string' || typeof hash !== 'string') return false;
  try {
    return await argon2.verify(hash, plain);
  } catch {
    return false;
  }
}
```

- [ ] In `ops/api/src/db.js` add `withTransaction()`:

```js
export async function withTransaction(fn) {
  const pool = getPool();
  const client = await pool.connect();
  try {
    await client.query('BEGIN');
    const result = await fn(client);
    await client.query('COMMIT');
    return result;
  } catch (error) {
    await client.query('ROLLBACK');
    throw error;
  } finally {
    client.release();
  }
}
```

- [ ] Commit: `Add argon2 password hashing and DB transaction helper`

---

## Task 3: Sessions store + auth middleware (TDD)

**Files:** Create `ops/api/src/auth/sessions.js`, `ops/api/src/middleware/auth.js`, `ops/api/test/auth-sessions.test.js`

- [ ] Write failing test `ops/api/test/auth-sessions.test.js`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import crypto from 'node:crypto';
import { getPool } from '../src/db.js';
import { createSession, loadSession, revokeSession } from '../src/auth/sessions.js';

const DB_URL = process.env.TEST_DATABASE_URL || 'postgres://ops:ops_dev_password@127.0.0.1:5433/ops';
process.env.DATABASE_URL = DB_URL;

async function ensureUser(email) {
  const pool = getPool();
  const res = await pool.query(
    `INSERT INTO auth_users (email, password_hash, role)
     VALUES ($1, 'hash', 'user')
     ON CONFLICT (email) DO UPDATE SET password_hash = EXCLUDED.password_hash
     RETURNING id`,
    [email]
  );
  return res.rows[0].id;
}

test('createSession returns id, persists session', async () => {
  const userId = await ensureUser(`session-test-${crypto.randomUUID()}@x.test`);
  const { id, expiresAt } = await createSession(userId, { ip: '1.2.3.4', userAgent: 'test' });
  assert.ok(id.length >= 32);
  assert.ok(expiresAt instanceof Date);
  const loaded = await loadSession(id);
  assert.equal(loaded.user_id, userId);
});

test('loadSession returns null for unknown id', async () => {
  const loaded = await loadSession('nonexistent-id');
  assert.equal(loaded, null);
});

test('revokeSession marks session as revoked', async () => {
  const userId = await ensureUser(`revoke-test-${crypto.randomUUID()}@x.test`);
  const { id } = await createSession(userId);
  await revokeSession(id);
  const loaded = await loadSession(id);
  assert.equal(loaded, null);
});
```

- [ ] Run `npm test` — expect failure (`Cannot find module '../src/auth/sessions.js'`).

- [ ] Implement `ops/api/src/auth/sessions.js`:

```js
import crypto from 'node:crypto';
import { getPool } from '../db.js';

const SESSION_TTL_DAYS = 60;

export async function createSession(userId, meta = {}) {
  const id = crypto.randomBytes(32).toString('hex');
  const expiresAt = new Date(Date.now() + SESSION_TTL_DAYS * 24 * 60 * 60 * 1000);
  const pool = getPool();
  await pool.query(
    `INSERT INTO auth_sessions (id, user_id, expires_at, ip, user_agent)
     VALUES ($1, $2, $3, $4, $5)`,
    [id, userId, expiresAt, meta.ip || null, meta.userAgent || null]
  );
  return { id, expiresAt };
}

export async function loadSession(id) {
  const pool = getPool();
  const res = await pool.query(
    `SELECT s.*, u.email, u.role, u.must_change_password, u.employee_id
       FROM auth_sessions s
       JOIN auth_users u ON u.id = s.user_id
      WHERE s.id = $1 AND s.revoked_at IS NULL AND s.expires_at > NOW()`,
    [id]
  );
  return res.rows[0] || null;
}

export async function revokeSession(id) {
  const pool = getPool();
  await pool.query(`UPDATE auth_sessions SET revoked_at = NOW() WHERE id = $1`, [id]);
}
```

- [ ] Run `npm test` — expect 3/3 pass.

- [ ] Create middleware `ops/api/src/middleware/auth.js`:

```js
import { loadSession } from '../auth/sessions.js';

export async function requireAuth(req, res, next) {
  const sessionId = req.cookies?.session_id;
  if (!sessionId) {
    return res.status(401).json({ error: { code: 'NO_SESSION', message: 'Не авторизован' } });
  }
  const session = await loadSession(sessionId);
  if (!session) {
    return res.status(401).json({ error: { code: 'INVALID_SESSION', message: 'Сессия истекла или отозвана' } });
  }
  req.user = {
    id: session.user_id,
    email: session.email,
    role: session.role,
    employeeId: session.employee_id,
    mustChangePassword: session.must_change_password,
  };
  next();
}

export function requireRole(...roles) {
  return (req, res, next) => {
    if (!req.user || !roles.includes(req.user.role)) {
      return res.status(403).json({ error: { code: 'FORBIDDEN', message: 'Недостаточно прав' } });
    }
    next();
  };
}
```

- [ ] Commit: `Add sessions store and auth middleware`

---

## Task 4: Auth routes (login/logout/me/change-password)

**Files:** Create `ops/api/src/routes/auth.js`, modify `ops/api/src/server.js`, create `ops/api/test/auth-routes.test.js`

- [ ] Create `ops/api/src/routes/auth.js`:

```js
import { Router } from 'express';
import { getPool } from '../db.js';
import { hashPassword, verifyPassword } from '../auth/argon.js';
import { createSession, revokeSession } from '../auth/sessions.js';
import { requireAuth } from '../middleware/auth.js';

const router = Router();

const COOKIE_OPTS = {
  httpOnly: true,
  secure: process.env.NODE_ENV === 'production',
  sameSite: 'lax',
  maxAge: 60 * 24 * 60 * 60 * 1000,
  path: '/',
};

router.post('/login', async (req, res) => {
  const email = String(req.body?.email || '').trim().toLowerCase();
  const password = String(req.body?.password || '');
  if (!email || !password) {
    return res.status(400).json({ error: { code: 'INVALID_INPUT', message: 'Email и пароль обязательны' } });
  }
  const pool = getPool();
  const userRes = await pool.query(
    `SELECT id, email, password_hash, role, must_change_password, employee_id
       FROM auth_users WHERE LOWER(email) = $1`,
    [email]
  );
  const user = userRes.rows[0];
  if (!user || !(await verifyPassword(password, user.password_hash))) {
    return res.status(401).json({ error: { code: 'INVALID_CREDENTIALS', message: 'Неверный email или пароль' } });
  }
  const { id: sessionId } = await createSession(user.id, {
    ip: req.ip,
    userAgent: req.get('user-agent'),
  });
  await pool.query(`UPDATE auth_users SET last_login_at = NOW() WHERE id = $1`, [user.id]);
  res.cookie('session_id', sessionId, COOKIE_OPTS);
  res.json({
    user: {
      id: user.id,
      email: user.email,
      role: user.role,
      employeeId: user.employee_id,
      mustChangePassword: user.must_change_password,
    },
  });
});

router.post('/logout', async (req, res) => {
  const sessionId = req.cookies?.session_id;
  if (sessionId) await revokeSession(sessionId);
  res.clearCookie('session_id', { path: '/' });
  res.status(204).end();
});

router.get('/me', requireAuth, (req, res) => {
  res.json({ user: req.user });
});

router.post('/change-password', requireAuth, async (req, res) => {
  const oldPassword = String(req.body?.old_password || '');
  const newPassword = String(req.body?.new_password || '');
  if (newPassword.length < 10) {
    return res.status(400).json({ error: { code: 'WEAK_PASSWORD', message: 'Минимум 10 символов' } });
  }
  const pool = getPool();
  const userRes = await pool.query(
    `SELECT password_hash FROM auth_users WHERE id = $1`,
    [req.user.id]
  );
  if (!(await verifyPassword(oldPassword, userRes.rows[0].password_hash))) {
    return res.status(400).json({ error: { code: 'WRONG_OLD_PASSWORD', message: 'Старый пароль неверный' } });
  }
  const newHash = await hashPassword(newPassword);
  await pool.query(
    `UPDATE auth_users SET password_hash = $1, must_change_password = FALSE WHERE id = $2`,
    [newHash, req.user.id]
  );
  res.json({ ok: true });
});

export default router;
```

- [ ] Modify `ops/api/src/server.js`:

```js
import express from 'express';
import cookieParser from 'cookie-parser';
import healthRoute from './routes/health.js';
import authRoute from './routes/auth.js';

export function createServer() {
  const app = express();
  app.set('trust proxy', 1);
  app.use(express.json());
  app.use(cookieParser());
  app.use('/api', healthRoute);
  app.use('/api/auth', authRoute);
  return app;
}
```

- [ ] Create `ops/api/test/auth-routes.test.js` — write integration tests:
  - POST /login with valid creds → 200, sets cookie, returns user
  - POST /login with wrong password → 401
  - GET /me without cookie → 401
  - GET /me with cookie → 200 with user
  - POST /logout → 204, clears cookie
  - POST /change-password updates hash

(Используй `fetch` с заголовком `cookie` для передачи session_id, как в Block 1 health.test.js)

- [ ] Run `npm test` — expect ALL tests pass.

- [ ] Manual smoke via curl:

```bash
# Создать тестового юзера
psql ... -c "INSERT INTO auth_users (email, password_hash) VALUES ('test@x.com', '<hash>') ON CONFLICT DO NOTHING;"
# Использовать ops/api/scripts/hash.mjs (см. Task 5) для генерации hash
curl -i -c /tmp/cookies.txt -X POST http://127.0.0.1:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@x.com","password":"testpass1234"}'
curl -b /tmp/cookies.txt http://127.0.0.1:3000/api/auth/me
curl -b /tmp/cookies.txt -X POST http://127.0.0.1:3000/api/auth/logout
```

- [ ] Commit: `Add auth routes: login, logout, me, change-password`

---

## Task 5: Скрипт копирования employees + выдачи временных паролей

**Files:** Create `ops/scripts/refresh/01-employees.mjs`, `ops/scripts/issue-temp-passwords.mjs`, `ops/api/scripts/hash.mjs`

- [ ] Create `ops/api/scripts/hash.mjs` — utility для генерации hash вручную:

```js
import { hashPassword } from '../src/auth/argon.js';
const password = process.argv[2];
if (!password) { console.error('Usage: node hash.mjs <password>'); process.exit(1); }
console.log(await hashPassword(password));
```

- [ ] Create `ops/scripts/refresh/01-employees.mjs`:

```js
// Копирует employees из Supabase в Postgres.
// Запуск: node ops/scripts/refresh/01-employees.mjs
// Требует переменные окружения:
//   SUPABASE_URL (https://jbpmorruwjrxcieqlbmd.supabase.co)
//   SUPABASE_SERVICE_KEY (читать https://supabase.com/dashboard/.../settings/api)
//   DATABASE_URL (postgres://ops:...@host:port/ops)

import { createClient } from '@supabase/supabase-js';
import pg from 'pg';

const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_KEY);
const { Pool } = pg;
const pool = new Pool({ connectionString: process.env.DATABASE_URL });

async function main() {
  console.log('Fetching employees from Supabase...');
  const { data, error } = await supabase.from('employees').select('*');
  if (error) throw error;
  console.log(`Found ${data.length} employees`);

  for (const e of data) {
    await pool.query(
      `INSERT INTO employees (id, name, email, role, hourly_rate, is_active, extras, created_at, updated_at)
       VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
       ON CONFLICT (id) DO UPDATE SET
         name = EXCLUDED.name,
         email = EXCLUDED.email,
         role = EXCLUDED.role,
         hourly_rate = EXCLUDED.hourly_rate,
         is_active = EXCLUDED.is_active,
         extras = EXCLUDED.extras,
         updated_at = EXCLUDED.updated_at`,
      [
        e.id,
        e.name || '',
        e.email || null,
        e.role || null,
        e.hourly_rate ?? null,
        e.is_active !== false,
        e.extras || {},
        e.created_at || new Date().toISOString(),
        e.updated_at || new Date().toISOString(),
      ]
    );
  }

  const { rows } = await pool.query(`SELECT COUNT(*) AS n FROM employees`);
  console.log(`Postgres now has ${rows[0].n} employees`);
  await pool.end();
}

main().catch(err => { console.error(err); process.exit(1); });
```

- [ ] Install `@supabase/supabase-js`:

```bash
mkdir -p ops/scripts
cd ops/scripts
npm init -y
npm install @supabase/supabase-js pg
```

(Make `ops/scripts/package.json` have `"type": "module"`)

- [ ] Create `ops/scripts/issue-temp-passwords.mjs`:

```js
// Для каждого active employee создаёт auth_user с временным случайным паролем.
// Выводит CSV в stdout: email,temporary_password
// Запуск: node ops/scripts/issue-temp-passwords.mjs > /tmp/temp-passwords.csv

import pg from 'pg';
import crypto from 'node:crypto';
import { hashPassword } from '../api/src/auth/argon.js';

const { Pool } = pg;
const pool = new Pool({ connectionString: process.env.DATABASE_URL });

function generatePassword() {
  // 16 hex chars = 64 bits entropy, читаемо
  return crypto.randomBytes(8).toString('hex');
}

async function main() {
  const { rows } = await pool.query(
    `SELECT id, email, name FROM employees WHERE is_active = TRUE AND email IS NOT NULL`
  );
  console.error(`Found ${rows.length} active employees with emails`);
  console.log('email,name,temp_password');
  for (const e of rows) {
    const pw = generatePassword();
    const hash = await hashPassword(pw);
    await pool.query(
      `INSERT INTO auth_users (email, password_hash, employee_id, role, must_change_password)
       VALUES ($1, $2, $3, 'user', TRUE)
       ON CONFLICT (email) DO UPDATE SET password_hash = EXCLUDED.password_hash, must_change_password = TRUE`,
      [e.email.toLowerCase(), hash, e.id]
    );
    console.log(`${e.email},${e.name},${pw}`);
  }
  await pool.end();
  console.error('Done. Распечатай CSV, выдай сотрудникам пароли по защищённому каналу.');
}

main().catch(err => { console.error(err); process.exit(1); });
```

- [ ] Manual run locally on staging DB:

```bash
SUPABASE_URL=https://jbpmorruwjrxcieqlbmd.supabase.co \
SUPABASE_SERVICE_KEY=<service-key-from-supabase-dashboard> \
DATABASE_URL=postgres://ops:ops_dev_password@127.0.0.1:5433/ops \
  node ops/scripts/refresh/01-employees.mjs
```

- [ ] Затем admin-юзер для себя:

```bash
HASH=$(cd ops/api && node scripts/hash.mjs YourStrongPassword)
psql postgres://ops:ops_dev_password@127.0.0.1:5433/ops -c \
  "INSERT INTO auth_users (email, password_hash, role, must_change_password) VALUES ('polinacherpovitskaya@gmail.com', '$HASH', 'admin', FALSE);"
```

- [ ] Commit: `Add employees copy script and temp password issuer`

---

## Task 6: Vue 3 — login screen + auth store

**Files:** Create `ops/web/src/api/auth.ts`, `ops/web/src/api/index.ts`, `ops/web/src/stores/auth.ts`, `ops/web/src/views/LoginView.vue`, `ops/web/src/views/ChangePasswordView.vue`, modify `ops/web/src/router.ts`, `ops/web/src/views/PlaceholderView.vue`

- [ ] Create `ops/web/src/api/index.ts` — общий fetch-wrapper:

```ts
export interface ApiError {
  code: string;
  message: string;
  details?: unknown;
}

export async function apiFetch<T>(
  path: string,
  init: RequestInit = {}
): Promise<T> {
  const headers = new Headers(init.headers);
  headers.set('Content-Type', 'application/json');
  if (init.method && init.method !== 'GET') {
    headers.set('Idempotency-Key', crypto.randomUUID());
  }
  const res = await fetch(path, { ...init, headers, credentials: 'include' });
  if (!res.ok) {
    const body = await res.json().catch(() => ({}));
    const err = (body.error as ApiError) || { code: 'HTTP_ERROR', message: `HTTP ${res.status}` };
    throw err;
  }
  if (res.status === 204) return undefined as unknown as T;
  return res.json();
}
```

- [ ] Create `ops/web/src/api/auth.ts`:

```ts
import { apiFetch } from './index';

export interface User {
  id: number;
  email: string;
  role: 'admin' | 'user';
  employeeId: number | null;
  mustChangePassword: boolean;
}

export async function login(email: string, password: string): Promise<User> {
  const { user } = await apiFetch<{ user: User }>('/api/auth/login', {
    method: 'POST',
    body: JSON.stringify({ email, password }),
  });
  return user;
}

export async function logout(): Promise<void> {
  await apiFetch('/api/auth/logout', { method: 'POST' });
}

export async function me(): Promise<User | null> {
  try {
    const { user } = await apiFetch<{ user: User }>('/api/auth/me');
    return user;
  } catch {
    return null;
  }
}

export async function changePassword(oldPassword: string, newPassword: string): Promise<void> {
  await apiFetch('/api/auth/change-password', {
    method: 'POST',
    body: JSON.stringify({ old_password: oldPassword, new_password: newPassword }),
  });
}
```

- [ ] Create `ops/web/src/stores/auth.ts`:

```ts
import { defineStore } from 'pinia';
import { ref, computed } from 'vue';
import * as authApi from '../api/auth';
import type { User } from '../api/auth';

export const useAuthStore = defineStore('auth', () => {
  const user = ref<User | null>(null);
  const isLoaded = ref(false);
  const isLoggedIn = computed(() => user.value !== null);

  async function loadMe() {
    user.value = await authApi.me();
    isLoaded.value = true;
  }

  async function login(email: string, password: string) {
    user.value = await authApi.login(email, password);
  }

  async function logout() {
    await authApi.logout();
    user.value = null;
  }

  async function changePassword(oldP: string, newP: string) {
    await authApi.changePassword(oldP, newP);
    if (user.value) user.value.mustChangePassword = false;
  }

  return { user, isLoaded, isLoggedIn, loadMe, login, logout, changePassword };
});
```

- [ ] Create `ops/web/src/views/LoginView.vue`:

```vue
<template>
  <main class="login">
    <h1>Вход</h1>
    <form @submit.prevent="handleSubmit">
      <label>Email <input v-model="email" type="email" required autofocus /></label>
      <label>Пароль <input v-model="password" type="password" required /></label>
      <button :disabled="loading">{{ loading ? '...' : 'Войти' }}</button>
      <p v-if="error" class="error">{{ error }}</p>
    </form>
  </main>
</template>

<script setup lang="ts">
import { ref } from 'vue';
import { useRouter, useRoute } from 'vue-router';
import { useAuthStore } from '../stores/auth';

const email = ref('');
const password = ref('');
const loading = ref(false);
const error = ref('');
const router = useRouter();
const route = useRoute();
const auth = useAuthStore();

async function handleSubmit() {
  loading.value = true;
  error.value = '';
  try {
    await auth.login(email.value, password.value);
    const next = (route.query.next as string) || '/';
    if (auth.user?.mustChangePassword) {
      router.push('/change-password');
    } else {
      router.push(next);
    }
  } catch (e: any) {
    error.value = e?.message || 'Ошибка входа';
  } finally {
    loading.value = false;
  }
}
</script>

<style scoped>
.login { max-width: 24rem; margin: 4rem auto; padding: 2rem; font-family: system-ui, sans-serif; }
.login form { display: flex; flex-direction: column; gap: 1rem; }
.login label { display: flex; flex-direction: column; gap: 0.25rem; }
.login input { padding: 0.5rem; font-size: 1rem; }
.login button { padding: 0.6rem; font-size: 1rem; background: #111; color: white; border: 0; cursor: pointer; }
.error { color: crimson; }
</style>
```

- [ ] Create `ops/web/src/views/ChangePasswordView.vue` (структура аналогичная LoginView, поля old/new/confirm, вызов `auth.changePassword`, redirect на `/`)

- [ ] Modify `ops/web/src/router.ts`:

```ts
import { createRouter, createWebHistory } from 'vue-router';
import { useAuthStore } from '../stores/auth';
import PlaceholderView from './views/PlaceholderView.vue';
import LoginView from './views/LoginView.vue';
import ChangePasswordView from './views/ChangePasswordView.vue';

const router = createRouter({
  history: createWebHistory(),
  routes: [
    { path: '/', name: 'home', component: PlaceholderView, meta: { requiresAuth: true } },
    { path: '/login', name: 'login', component: LoginView },
    { path: '/change-password', name: 'change-password', component: ChangePasswordView, meta: { requiresAuth: true } },
  ],
});

router.beforeEach(async (to) => {
  const auth = useAuthStore();
  if (!auth.isLoaded) await auth.loadMe();
  if (to.meta.requiresAuth && !auth.isLoggedIn) {
    return { name: 'login', query: { next: to.fullPath } };
  }
  if (auth.user?.mustChangePassword && to.name !== 'change-password') {
    return { name: 'change-password' };
  }
});

export default router;
```

- [ ] Modify `ops/web/src/views/PlaceholderView.vue` — добавить плашку "Привет, {email}" и кнопку "Выйти".

- [ ] Run `npm run dev` + проверить флоу вручную: открыть `/`, должен редиректнуть на `/login`, залогиниться, попасть на `/change-password` (т.к. must_change_password=true), сменить пароль, попасть на `/`.

- [ ] Commit: `Add Vue login flow with auth store`

---

## Task 7: Endpoint /api/employees + добавить refresh в CI

**Files:** Create `ops/api/src/routes/employees.js`, `ops/api/test/employees.test.js`, modify `ops/api/src/server.js`

- [ ] Write tests in `ops/api/test/employees.test.js`:
  - GET без cookie → 401
  - GET с cookie → 200, массив с правильной формой
  - GET от обычного юзера видит только себя или активных (зависит от роли)

- [ ] Implement `ops/api/src/routes/employees.js`:

```js
import { Router } from 'express';
import { getPool } from '../db.js';
import { requireAuth } from '../middleware/auth.js';

const router = Router();

router.get('/', requireAuth, async (req, res) => {
  const pool = getPool();
  const onlyActive = String(req.query.active || 'true') !== 'false';
  const sql = onlyActive
    ? `SELECT id, name, email, role, hourly_rate, is_active FROM employees WHERE is_active = TRUE ORDER BY name`
    : `SELECT id, name, email, role, hourly_rate, is_active FROM employees ORDER BY name`;
  const { rows } = await pool.query(sql);
  res.json({ employees: rows });
});

export default router;
```

- [ ] Wire up in `ops/api/src/server.js`: `app.use('/api/employees', employeesRoute);`

- [ ] Run tests, expect green.

- [ ] Commit: `Add /api/employees endpoint with auth guard`

---

## Task 8: Обновить CI workflow для миграций + интеграционных тестов

**Files:** Modify `.github/workflows/ops-deploy.yml`

- [ ] В CI workflow убедиться, что после apply миграций (002_auth.sql) — тесты auth прогоняются (они уже подхватятся через `npm test` если ты не менял тестовую конфигурацию).

- [ ] Run lint + test локально перед коммитом:

```bash
cd ops/api && npm test
cd ops/web && npm run build
```

- [ ] Commit: `Block 2: auth complete`

---

## Verification

После всех задач:

```bash
# 1. Локально логин работает
cd ops/api && DATABASE_URL=... node src/index.js &
curl -i -c /tmp/c.txt -X POST http://127.0.0.1:3000/api/auth/login \
  -H "Content-Type: application/json" -d '{"email":"polinacherpovitskaya@gmail.com","password":"YourStrongPassword"}'
# должен вернуть 200 и Set-Cookie

# 2. /api/employees защищён
curl http://127.0.0.1:3000/api/employees                    # 401
curl -b /tmp/c.txt http://127.0.0.1:3000/api/employees      # 200 + список

# 3. После деплоя — https://ops-staging.recycleobject.ru:
# открыть в браузере, увидеть форму логина, залогиниться
```

## Acceptance Criteria

- [ ] Все тесты в `ops/api/test/` зелёные
- [ ] `https://ops-staging.recycleobject.ru/login` показывает форму
- [ ] Залогиниться как admin → попасть на placeholder (без редиректа на change-password)
- [ ] Залогиниться как обычный сотрудник → попасть на change-password
- [ ] После смены пароля — login повторно работает с новым
- [ ] `/api/employees` без cookie возвращает 401
- [ ] PR создан в `main`, CI зелёный
